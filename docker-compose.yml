version: "3.9"

services:
  ticket-alert:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./profiles:/app/profiles
      - ./screenshots:/app/screenshots

